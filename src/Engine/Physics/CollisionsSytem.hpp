// #pragma once
// #include "../Model/Entities/Entity.hpp"
// #include <vector>

// class CollisionSystem {
// public:
//     CollisionSystem() = default;
//     ~CollisionSystem() = default;

//     // Ajoute une entité à surveiller pour les collisions
//     void addEntity(Entity* entity);

//     // Vérifie les collisions entre l'entité player et les autres
//     void handleCollisions(Entity& player);

// private:
//     std::vector<Entity> entities;
// };
